@{
    ViewData["Title"] = "GRate - Вход в систему";
}
@model GRate.ViewModel.LoginModel

<div id="overlay"></div>
<div id="log" class="glassmorph logzone">
    <div class="logzone-2">
        <form asp-action="Login" asp-controller="Account" asp-anti-forgery="true">
            <img src="~/res/logo.png" width="64" height="64" alt="logo" style="margin-top:5%; margin-bottom:5%; user-select: none" />
            <h5 style="color:#fff; cursor:default;user-select: none;">Войдите в свой аккаунт</h5>
            <input class="Logtext" asp-for="Login" placeholder="Логин" />
            <div class="password" style="position: relative;">
                <input class="Logtext" asp-for="Password" id="password-input" placeholder="Пароль" />
                <a class="password-control" onclick="return show_hide_password(this);"></a>
            </div>
            <div class="exeption" style=" font-weight: bold; height: 1rem"></div>
            <div class="Twobutts">
                <div>
                    <p style="margin:0;user-select: none;">Я новенький</p>
                    <button asp-controller="Account" asp-action="Register" class="logbuttons">Зарегистрироваться</button>
                </div>
                <div>
                    <p style="margin:0;user-select: none;">Я уже смешарик</p>
                    <button type="submit" class="logbuttons">Войти в аккаунт</button>
                </div>
            </div>
            @if (ViewBag.message != null)
            {
                <p style="font-weight: bold; color:#f61a1a">Ой, что-то пошло не так...</p>
            }
        </form>
    </div>
</div>

<script>
    function show_hide_password(target) {
        var input = document.getElementById('password-input');
        if (input.getAttribute('type') == 'password') {
            target.classList.add('view');
            input.setAttribute('type', 'text');
        } else {
            target.classList.remove('view');
            input.setAttribute('type', 'password');
        }
        return false;
    }
</script>
